services:
  workspace:
    build:
      context: .
    volumes:
      - ..:/workspace:cached
      - ${CLAUDE_CONFIG_USER_DIR:-~/.claude}:/home/node/.claude
    environment:
      TZ: "${TZ:-America/Los_Angeles}"
    command: sleep infinity
